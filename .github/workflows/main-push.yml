name: Main Branch CI

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-and-package:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run lint and format check
        run: npm run check
        
      - name: Run tests
        run: npm test
        
      - name: Run build
        run: npm run build
        
      - name: Package extension
        run: npm run package
        
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: extension-package
          path: |
            dist/
            line-docs-to-mkdwn.zip
          retention-days: 30

      - name: Get version from package.json
        id: get_version
        run: echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Create Git tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ steps.get_version.outputs.version }}" -m "Release v${{ steps.get_version.outputs.version }}"
          git push origin "v${{ steps.get_version.outputs.version }}"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "v${{ steps.get_version.outputs.version }}" \
            line-docs-to-mkdwn.zip \
            --title "v${{ steps.get_version.outputs.version }}" \
            --generate-notes

      # Chrome Web Storeへアップロードと公開
      # 注意: コマンドなしで実行することで、アップロードと公開の両方を自動実行
      - name: Upload and Publish to Chrome Web Store
        run: |
          npx chrome-webstore-upload-cli@3.5.0 --source line-docs-to-mkdwn.zip --extension-id $EXTENSION_ID
        env:
          EXTENSION_ID: ${{ secrets.CHROME_EXTENSION_ID }}
          CLIENT_ID: ${{ secrets.CHROME_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET }}
          REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN }}

      - name: Deployment summary
        run: |
          echo "✅ Chrome Web Storeへのアップロードと公開が完了しました"
          echo "📦 バージョン: v${{ steps.get_version.outputs.version }}"
          echo "🔗 デベロッパーダッシュボード: https://chrome.google.com/webstore/devconsole"
          echo "🎉 自動的に公開されました。Chrome Web Storeで数分以内に利用可能になります。"